<Application x:Class="AliceCoopLauncher.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="WindowBrush" Color="#17171D" />
        <SolidColorBrush x:Key="PanelBrush" Color="#23232C" />
        <SolidColorBrush x:Key="PanelLightBrush" Color="#2B2B36" />
        <SolidColorBrush x:Key="AccentBrush" Color="#B49AF7" />
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#C8B6FA" />
        <SolidColorBrush x:Key="TextBrush" Color="#F3F0FA" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#AAA6B5" />
        <SolidColorBrush x:Key="SuccessBrush" Color="#79D6A3" />
        <SolidColorBrush x:Key="WarningBrush" Color="#E5C07B" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#18181F" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderBrush" Value="#4A4658" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="FontSize" Value="14" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#18181F" />
            <Setter Property="Foreground" Value="#17171D" />
            <Setter Property="BorderBrush" Value="#4A4658" />
            <Setter Property="Padding" Value="8,5" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#353342" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="#565166" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="#17151D" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="Padding" Value="18,11" />
            <Setter Property="FontSize" Value="15" />
        </Style>
    </Application.Resources>
</Application>
